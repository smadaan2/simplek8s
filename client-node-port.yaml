apiVersion: v1     #limits the type of objects we want to create by giving specific configuration file
kind: Service      #setup networking
metadata: 
  name: client-node-port
spec:
  type: NodePort  
  ports: 
    - port: 3050
      targetPort: 3000
      nodePort: 31515
  selector:
     component: web    